Review all files I changed since the last Git commit.  For each file, check: 1. Bugs or logic errors 2. Security issues (exposed secrets, missing validation) 3. Performance problems 4. Missing error handling 5. Style violations (check CLAUDE.md if it exists)  For each issue: state the file name, what's wrong, and how to fix it. If everything looks good, say so. Don't invent problems.